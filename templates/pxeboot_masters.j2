UI menu.c32 
PROMPT 0
TIMEOUT 15
MENU TITLE PXE BOOT MENU

LABEL MASTER NODE
  MENU LABEL Install MASTER
  MENU DEFAULT
  KERNEL {{ downloads.tftp_workspace_dir }}/{{ installer_kernel }}
  APPEND rd.neednet=1 initrd=rhcos/{{ installer_initramfs }} console=tty0 coreos.inst=yes coreos.inst.install_dev=vda coreos.inst.ignition_url=http://{{  networking.internal_network_ip }}/{{ downloads.nginix_workspace_dir }}/master.ign coreos.inst.image_url=http://{{ networking.internal_network_ip }}/{{ downloads.nginix_workspace_dir }}/{{ coreos }} ip=dhcp



